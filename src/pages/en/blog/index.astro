---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog', (p) => p.id.startsWith('en/') && !p.data.draft))
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());

const fmt = new Intl.DateTimeFormat('en-US', { year: 'numeric', month: '2-digit', day: '2-digit', timeZone: 'UTC' });
---
<BaseLayout title="Blog - Marco Sánchez Sorondo" description="Notes on AI, development, research, consulting, and training.">
  <header>
    <p><a href="/en/">Marco Sánchez Sorondo</a> / blog / <a href="/es/blog/">ES</a></p>
    <h1>Blog</h1>
  </header>
  <main>
    <hr />
    {posts.length > 0 ? (
      <ul class="rows">
        {posts.map((p) => (
          <li>
            <span class="when">{fmt.format(p.data.pubDate)}</span>{' '}
            <a href={`/en/blog/${p.id.replace(/^en\//, '')}/`}>{p.data.title}</a>
            <p class="mute">{p.data.description}</p>
          </li>
        ))}
      </ul>
    ) : (
      <p class="mute">First posts coming soon.</p>
    )}
    <p><a href="/en/rss.xml">RSS</a></p>
  </main>
</BaseLayout>
