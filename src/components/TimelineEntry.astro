---
interface Props {
  role: string;
  org: string;
  startDate: Date;
  endDate: Date | null;
  kind: 'research' | 'applied' | 'teaching';
  bullets: string[];
  location: string;
}
const { role, org, startDate, endDate, kind, bullets, location } = Astro.props;
const fmt = (d: Date) => d.toLocaleDateString('en-US', { year: 'numeric', month: 'short' });
const kindLabel: Record<string, string> = { research: 'Investigación', applied: 'Aplicado', teaching: 'Docencia' };
---
<article class="border-l border-line pl-6 pb-10 relative">
  <span class="absolute -left-[5px] top-1 w-2.5 h-2.5 bg-ink rounded-full" aria-hidden="true"></span>
  <p class="text-xs uppercase tracking-widest text-ink-mute">{fmt(startDate)} — {endDate ? fmt(endDate) : 'Presente'} · {kindLabel[kind]} · {location}</p>
  <h3 class="font-display text-xl mt-2">{role}</h3>
  <p class="text-ink-soft">{org}</p>
  <ul class="mt-3 space-y-1 text-sm list-disc list-inside marker:text-ink-mute">
    {bullets.map(b => <li>{b}</li>)}
  </ul>
</article>
